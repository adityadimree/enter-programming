def square(number) :
    return number**2

print(square(32049))
#Right now  what is actually happening is that jese hi computer detects square(somethting), it sees the definition which we gave in the above line. or usme pront krne ko bole toh use vo simply print kr deta hai.